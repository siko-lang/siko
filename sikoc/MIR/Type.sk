module Siko.MIR.Type where

import Siko.MIR.Program

data Type = Adt Identity
            | Record Identity
            | Ref Type
            | Never

instance Show Type where
    show t = case t of
        Adt id -> show id
        Record id -> show id
        Ref t -> "&{}" % t
        Never -> "!"
