module Main where

data Boo = Boo

data OneRecord = { i :: Boo }

data Foo = One OneRecord
         | Two

foo f = case f of
            One record -> One (record { i = Boo })
            t -> t


main = do
    foo Two
    ()