module Main where

import Map
import List2

data RecordField = RecordField

data Context = { }

mapType :: Context -> Context
mapType context = do
    fields :: List2 RecordField <- List2.empty
    List2.fold context fields (\context, f -> mapType context)

main = do
    mapType Context
    ()