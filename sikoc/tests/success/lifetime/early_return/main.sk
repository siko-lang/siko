module Main where

data Context = Context deriving (Move)

func :: Context -> Context
func context  = do
    if True
    then return context
    else ()
    context

main = do
    context <- Context
    if True
    then func context
    else Context
    ()