module Int where

data Int = extern

module List where

data List a = extern

remove a :: ~[~a] -> ~Int -> ~(~a, ~[~a])
remove l index = extern

module Main where

main = do
    a <- List.remove [0] 1