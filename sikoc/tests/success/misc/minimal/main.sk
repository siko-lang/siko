// module Hack where

// sink a :: ~a -> ~()
// sink a = extern

module Main where

import Hack

data Foo = Foo

data Rec = { foo :: Foo }

main = do
    r <- Rec Foo
    sink r.foo
    sink r.foo
    ()
