module Main where

data Outer = Outer Inner

data Inner = Inner
data Season = Spring | Summer | Fall | Winter

id a = a

data Foo = Foo Inner | Bar

data Rec = { inner :: Inner }

main = do
    r <- Rec Inner
    ri <- r.inner
    ()
