module Main where

import Rc

data Foo = Foo

main = do
    bool <- True
    rc_b <- Rc.create bool
    foo <- Foo
    rc_f <- Rc.create foo
    b <- Rc.get rc_f
    if True
    then foo
    else b
    b
    rc_f
    foo
    case bool of
        True -> ()
        _ -> ()