module Main where

data Outer = Outer Inner

data Inner = Inner
data Season = Spring | Summer | Fall | Winter

id a = a

data Foo = Foo Inner | Bar

access a = a

data Rec = { inner :: Inner, inner2 :: Inner  }

main = do
    r <- Rec Inner Inner
    access r.inner
    access r.inner
    access r.inner
    //r { inner2 = Inner }
    ()
