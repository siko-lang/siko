module Int where

data Int = extern

module String where

data String = extern

module Main where

adder :: Int -> Int -> (Int, Int)
adder a b = (a, a)

less :: Int -> Int -> (Int, Int)
less a = adder a

main = do
    b <- less 1 2
    a <- adder
    a 3 4
    a <- adder 2
    b <- a 2