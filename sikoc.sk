module Int where

data Int = extern

module String where

data String = extern

module Main where

add1 :: Int -> Int -> (Int, Int)
add1 a b = (a, b)

add2 :: Int -> Int -> Int -> (Int, Int)
add2 a b c = (a, b)

fn :: (Int -> (Int, Int)) -> (Int, Int)
fn f = f 2

main = do
    a <- add1 1
    b <- add2 1 2
    fn a
    fn b
    ()