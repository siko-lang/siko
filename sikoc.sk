module Int where

data Int = extern

module String where

data String = extern

module Main where

adder :: Int -> Int -> (Int, Int)
adder a b = (a, a)

main = do
    a <- adder
    a 3 4
    a <- adder 2
    b <- a 2