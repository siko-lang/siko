module Int where

data Int = extern

module String where

data String = extern

module Main where

adder2 :: Int -> Int -> Int -> (Int, Int)
adder2 a b c = (a, a)

main = do
    a <- adder2 1
    b <- a 2
    a <- a 2
    a <- a 3