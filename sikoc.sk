module Main where

import Std.Util

data Color = Color

instance PartialEq Color where
    opEq a b = True

instance Eq Color

instance PartialOrd Color where
    partialCmp a b = Some Equal

instance Ord Color where
    cmp a b = Equal

main = ()