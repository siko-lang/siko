module Std.Ops where

class Add a where
    opAdd a :: a -> a -> a

module Int where

data Int = extern

module Main where

data Foo = Foo
data Boo = Boo

data Record = { foo :: Foo, boo :: Boo }

pll a = (a.0, a.3 + a.6)

main = do
    a <- (1,)
    a.0
