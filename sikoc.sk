module Int where

data Int = extern

module String where

data String = extern

module Main where

adder :: Int -> Int -> (Int, Int)
adder a b = (a, a)

adder2 :: Int -> Int -> Int -> (Int, Int)
adder2 a b c = (a, a)

less :: Int -> Int -> (Int, Int)
less a = adder a

data Foo = { func :: Int -> Int -> (Int, Int) }

data Alma = Korte | Szilva

main = do
    Korte
    q <- Foo less
    b <- q.func 1 2
    a <- adder
    a <- adder2 2
    a 3 4
    a <- adder 2
    b <- a 2