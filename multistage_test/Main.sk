module Main where

data Res a b = Ok a | Err b

data ParserError = ParserError

data ParserState = {}

parseModule :: ParserState -> Res ParserState (ParserState, ParserError)
parseModule state = do
    Ok state

main = do
    parseModule ParserState
    ()
