module Std.Ops where

class Add a where
    opAdd a :: a -> a -> a

module Int where

data Int = extern

instance Add Int where
    opAdd a b = addInt a b

addInt :: ~Int -> ~Int -> ~Int
addInt a b = extern

module Main where

main = do
    1 + 1
    ()
