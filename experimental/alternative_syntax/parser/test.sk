module Main {

import List
import List
import List
import List.Korte

enum Enum {
    One(String),
    Two((Record, Enum)),
    FooHolder(Foo)
}

enum Enum {
    One(String)
}

struct String { }

struct Record { 
    foo: String,
    bar: Enum
}

struct Foo { }

trait Korte {
    fn foo(self) -> ();

    fn foo(self, a: Foo, self, self) -> () {
        alma()
    }
}

effect Korte {
    fn foo() -> ();

    fn foo() -> ();

    fn foo() -> () {
        alma()
    }

    fn foo() -> () {
        alma()
    }

    fn foo() -> () {
        alma()
    }
}

fn simple_int1() -> Foo {
    {
        {
            {
                5
            }
        }
    }
}

fn simple_int2(record: Record) -> Foo {
    let a = 4;
    let ggf = 43424;
    let q = {
        let f = 4;
        4444
    };
    {
        4;
        {
            {
                5
            }
            {
                5
            }
        }
    }
    if bla() {
        5
    } else {
        4
    }
    loop a = 5 {
        break a;
    }
    foo.bar();
    other(5, bla(5), foo(), if 5 {
        a()
    } else {
    }, match 5 {
        5 => 4,
        _ => 3,
    }, loop a = 4 {
        5
    });
    5
}

fn simple_int3(record: Record, bla: BlaBla) -> Foo {
    5
}

fn other(record: Record) -> Foo {
    let f = match record.bar {
        One (a) => Foo(),
        FooHolder (f) => |f, g| {
                    foo(Foo(()))
                },
        _ => Foo(),
    };
}

fn main() -> () {
    let record = Record { foo = "a",
                          bar = One("foo"),
                          goo = 5,
                          fa = 5,
                          ggg = 5 };
    other(record)
}

}
