module Main {

enum Bool {
    True,
    False
}

enum Option[T] {
    Some(T),
    None
}

fn main() {
    let b1 = Main.Bool.True;
    let b2 = Bool.True;
    let b3 = True;
    let f: fn(Int) -> Option[Int] = Option.Some;
    let o = Option.Some(1);
    let f2: fn(Int) -> Option[Int] = Some;
    let o2 = Some(1);
}

}