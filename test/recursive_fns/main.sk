module Bool {

class Bool {

}

}
module Main {

class Foo {
    
}

fn foo(f: Foo) -> Foo {
    if true {
        foo(f)
    } else {
        Foo()
    }
}

fn one(f: Foo) -> Foo {
    if true {
        two(f)
    } else {
        two(f)
    }
}

fn two(f: Foo) -> Foo {
    if false {
        f
    } else {
        two(f) 
    }
}

fn main() {
    let f = Foo();
    one(f);
    foo(f);
}
}