module Main {

class Address {
    city: String
    street: String
}

class Person {
    name: String
    address: Address
}

class String {

}

fn main() {
    let address = Address(String(), String());
    let person = Person(String(), address);
    person.address.city;
    person.address;
    other();
    other2();
}

fn other() {
    let address = Address(String(), String());
    let person = Person(String(), address);
    if true {
        person.address.city;
    } else {
        person.address;
    }
}

fn other2() {
    let address = Address(String(), String());
    let person = Person(String(), address);
    if true {
        person.address.city;
    } else {
        person.address;
    }
    person;
}

}
     
