module Main {

implicit message: String

trait Doer[T] {
    fn doit(value: T) : <>
}

struct Thing {}

instance Doer[Thing] {
    fn doit(value: Thing) {
        println(message)
    }
}

fn main() {
    let current = "hello";
    with message = current {
        Thing().doit()
    }
}

}
