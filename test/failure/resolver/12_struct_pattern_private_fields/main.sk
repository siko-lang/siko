module Foo {

pub struct Point {
    pub x: I64,
    y: I64,

    pub fn new(x: I64, y: I64) -> Point {
        Point(x: x, y: y)
    }
}

}

module Main {

import Foo

fn main() {
    let p = Foo.Point.new(1, 2);
    match p {
        Foo.Point(a, b) => {}
    }
}

}
