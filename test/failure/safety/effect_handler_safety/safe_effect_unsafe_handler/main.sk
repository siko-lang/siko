module Main {

effect Action {
    @safe
    fn perform() {
        let size = sizeof(Int);
    }
}

@unsafe
fn unsafe_handler() {
    let size = sizeof(Int);
}

fn main() {
    with perform = unsafe_handler {
        perform();
    }
}

}
