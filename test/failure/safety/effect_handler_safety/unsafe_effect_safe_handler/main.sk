module Main {

effect Action {
    @unsafe
    fn perform() {
        let size = sizeof(Int);
    }
}

@safe
fn safe_handler() {
    let size = sizeof(Int);
}

@safe
fn main() {
    with perform = safe_handler {
        perform();
    }
}

}
