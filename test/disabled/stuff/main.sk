module String2 {

@derive(Clone)
struct String2 {

}

}

module Main {

import String2

struct Address {
    city: String2
    street: String2
}

struct Person {
    name: String2
    address: Address
}

struct Unit {

}

fn id(address: Address) -> String2 {
    if True {
        let address = Address(address.city, String2());
        let person = Person(String2(), address);
        person.address.city
    } else {
        let address = Address(String2(), address.street);
        let person = Person(String2(), address);
        person.address.city
    }
}

fn eat(a: Unit) {

}

fn main() {
    let address = Address(String2(), String2());
    id(address);
    address;
}

}
     
