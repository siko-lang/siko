module Std.Ops {

pub trait Clone[T] {
    fn clone(&self) -> T
}

pub trait Copy[T]

pub trait Drop[T] {
    fn drop(self) -> Self
}

}

module Bool {
pub enum Bool {
    True,
    False,
}
}

module Main {

struct Bar {

}

fn good() {}

fn bad() {}

fn main() {
    match Bar() {
        _ -> {
            if True {
                good();
            } ((), ())
        }
    }
}

}
