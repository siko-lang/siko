module Main {

class Object {
    value: Int
    fn set(self, a: Int) -> Self {
        Object(a)
    }
}

fn main() {
    let mut a = 1;
    assert(a.eq(1));
    a = 2;
    assert(a.eq(2));
    let mut o1 = Object(1);
    assert(o1.value.eq(1));
    o1.set(2);
    //assert(o1.value.eq(2));
}

}