module Main {

struct Score {
    value: Int
}

instance PartialEq[Score] = auto(PartialEq)
instance PartialOrd[Score] = auto(PartialOrd)

fn main() {
    let a = Score(10);
    let b = Score(20);
    if a < b { println("lt") };
    if b > a { println("gt") };
}

}
