module Main {

struct Pair {
    first: Int,
    second: Int
}

trait MyFold[T] {
    fn my_fold(a: T)
}

instance MyFold[Int] {
    fn my_fold(a: Int) {
        println(a)
    }
}

instance MyFold[Pair] = auto2(Fold)

fn main() {
    let p = Pair(3, 7);
    my_fold(p);
}

}
