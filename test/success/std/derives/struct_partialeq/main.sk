module Main {

struct Point {
    x: Int,
    y: Int
}

instance PartialEq[Point] = auto(PartialEq)

fn main() {
    let a = Point(1, 2);
    let b = Point(1, 2);
    let c = Point(1, 3);
    if a == b { println("eq") };
    if a != c { println("neq") };
}

}
