module Main {

enum Level {
    Low,
    Medium,
    High
}

instance PartialEq[Level] = auto(PartialEq)
instance PartialOrd[Level] = auto(PartialOrd)

fn main() {
    let a = Level.Low;
    let b = Level.High;
    if a < b { println("lt") };
    if b > a { println("gt") };
}

}
