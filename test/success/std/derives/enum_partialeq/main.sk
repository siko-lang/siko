module Main {

enum Color {
    Red,
    Green,
    Blue
}

instance PartialEq[Color] = auto

fn main() {
    let r1 = Color.Red;
    let r2 = Color.Red;
    let b = Color.Blue;
    if r1 == r2 { println("eq") };
    if r1 != b { println("neq") };
}

}
