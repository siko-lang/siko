module Main {

fn fib(n: Int) -> Int {
    if n <= 1 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn main() -> () {
    let x = 10;
    let y = fib(x);
    assert(y == 55);
    println(y.to_string());
}

}