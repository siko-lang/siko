module Main {

struct Point {
    x: Int,
    y: Int,

    pub fn scale(p: Point, mul: Int, offset: Int) -> Int {
        p.x * mul + offset
    }
}

fn main() {
    let p = Point(x: 3, y: 4);
    println("${p.scale(mul: 10, offset: 5)}");
    println("${p.scale(10, offset: 7)}");
}

}
