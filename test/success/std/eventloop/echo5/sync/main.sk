module Main {

import App
import Std.Time
import Std.Effects.Spawn
import Std.Effects.Network
import Std.Effects.Sleep

fn main() {
    let port = 19876.as_u16();
    with TcpNetwork = Tcp, Spawn = ThreadSpawn, Sleep = ThreadSleep {
        spawn(|| server(port));
        spawn(|| client(port));
        Time.sleep(Time.seconds(1));
    }
}

}
