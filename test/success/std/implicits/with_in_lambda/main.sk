module Main {

implicit value: Int

struct Runner {
    run: fn() -> ()
}

fn main() {
    let runner = Runner(| | {
        let local = 42;
        with value = local {
        }
    });
    (runner.run)();
}

}
