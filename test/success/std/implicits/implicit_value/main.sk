module Main {

implicit stuff : Int

fn foo() {
    stuff = stuff + 1;
}

fn main() {
    let data = 0
    with stuff = data {
        foo();
    }
    println(data.to_string());
    assert(data == 1);
}

}