module Main {

struct Config {
    name: String
}

implicit config: Config

fn foo() {
    println("config ${config.name}");
}

fn main() {
    let c = Config("myconfig");
    let f: fn() -> () = with config = c {
        || {
            foo();
        }
    };
    f();
}

}