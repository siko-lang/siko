module Main {

struct Value {
    get: fn() -> Int
}

struct Factory {
    make: fn() -> Value
}

fn main() {
    let factory = Factory(|| Value(|| 42));
    let make = factory.make;
    let value = make();
    let get = value.get;
    println(get());
}

}
