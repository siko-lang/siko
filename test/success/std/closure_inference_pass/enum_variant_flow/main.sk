module Main {

enum Wrap {
    Item(fn() -> ())
}

fn extract(w: Wrap) -> fn() -> () {
    match w {
        Wrap.Item(f) => f,
    }
}

fn main() {
    let w = Wrap.Item(|| {});
    let f = extract(w);
}

}
