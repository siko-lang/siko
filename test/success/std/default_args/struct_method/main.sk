module Main {

struct Counter {
    value: Int,

    pub fn new() -> Counter {
        Counter(0)
    }

    pub fn add(c: Counter, n: Int, step: Int = 1) -> Int {
        c.value + n * step
    }
}

fn main() {
    let c = Counter.new();
    println(c.add(3));
    println(c.add(2, 10));
}

}
