module Main {

fn main() {
    let v = match if {
        ('a'..'c').contains('b') -> 1,
        _ -> 2,
    };
    assert(v == 1);
    let v = match if 'b' {
        'b' -> 1,
        _ -> 2,
    };
    assert(v == 1);
}

}