module Main {

struct Foo {

}

instance Iterator[Foo] {
    type Item = ((), ())

    fn next(self) -> (Self, Option[((), ())]) {
        (self, Some(((), ())))
    }
}

fn main() {
    let f = Foo();
    for (i, i2) in f {
        break;
    }
}

}
