module Main {

struct Foo {

}

impl FooIterator Iterator[Foo] {
    type Item = ((), ())

    fn next(self) -> (Self, Option[((), ())]) {
        (self, Some(((), ())))
    }
}

fn main() {
    let f = Foo();
    for (i, i2) in f {
        break;
    }
}

}
