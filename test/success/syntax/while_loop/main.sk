module Main {

fn main() {
    let items = [1, 2, 3, 4, 5];
    let mut sum = 0;
    let mut i = 0;
    while i < items.len() {
        sum = sum + items.get(i);
        i = i + 1;
    }
    assert(sum == 15);
}

}