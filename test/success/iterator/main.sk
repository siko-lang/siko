module Main {

class Foo {
    value: Int
}

fn main() {
    let mut items = Vec.new();
    items.push(Foo(3));
    items.push(Foo(4));
    let mut i = items.iter();
    match i.next() {
        Some(v) -> assert(v.value == 3),
        None -> assert(False)
    }
    items.drop();
}

}