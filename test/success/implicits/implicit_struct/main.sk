module Main {

struct Foo {
    name: String
}

implicit stuff : Foo

fn foo() {
    stuff = Foo("bar");
}

fn main() {
    let data = Foo("baz");
    with stuff = data {
        foo();
    }
    assert(data.name == "bar");
    println(data.name);
}

}