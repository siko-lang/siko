module Settings {

pub implicit current: Int

}

module Main {

import Settings

fn main() {
    let value = 42;
    with Settings.current = value {
        let observed = Settings.current;
        println(observed.to_string());
        assert(observed == value);
    }
}

}
