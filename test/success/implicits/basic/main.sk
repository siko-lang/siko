module Main {

struct Data {
    name: String
}

implicit stuff : Data

fn foo() {
    stuff.name = "foo";
}

fn main() {
    let data = Data("Bar");
    with stuff = data {
        foo();
    }
    assert(data.name == "foo");
    println(data.name);
}

}