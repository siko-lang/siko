module Lib {

pub trait Show[T] {
    fn show(value: T) -> ()
}

pub instance MyShow Show[String] {
    fn show(value: String) -> () {
        println(value)
    }
}
}

module Main {
import Lib

fn main() -> () {
    Show.show("hello from named instance")
}
}
