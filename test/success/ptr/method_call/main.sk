module Main {

struct Foo {
    value: Int,

    fn bar(&self) -> Int {
        self.value
    }
}

@safe
fn main() {
    let foo = Foo(41);
    let result = foo.bar();
    assert(result == 41);
    let fooPtr = &raw foo;
    let resultPtr = fooPtr.bar();
    assert(resultPtr == 41);
}

}