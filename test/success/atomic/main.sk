module Main {

import Sync.Atomic

fn main() {
    let atomic = AtomicU32.new(0.toU32());
    assert(atomic.load() == 0.toU32());
    atomic.store(42.toU32());
    assert(atomic.load() == 42.toU32());
}

}