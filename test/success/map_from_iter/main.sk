module Main {

import Std.Map

fn main() {
    let m : Map[_, _] = [(0, "zero"), (1, "one"), (2, "two")].into_iter().collect();
    assert(m.get(0) == Some("zero"));
    assert(m.get(1) == Some("one"));
    assert(m.get(2) == Some("two"));
    assert(m.get(3) == None);
}

}
