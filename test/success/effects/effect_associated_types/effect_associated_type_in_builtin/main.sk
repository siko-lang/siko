module Main {

import Std.Intrinsics

effect Clock {
    type Instance
}

instance RealClock Clock {
    type Instance = Int
}

@safe
fn run() {
    let value: Clock.Instance = transmute(0);
}

fn main() {
    with Clock = RealClock {
        run();
    }
}

}
