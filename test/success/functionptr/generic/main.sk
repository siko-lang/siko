module Main {

fn main() {
    show("foo");
    generic(5);
    generic(False);
}

@safe
fn generic[T: ToString[T]](t: T) {
    let f: fn*(T) -> () = show;
    f(t);
}

fn show[T: ToString[T]](d: T) -> () {
    let s = d.to_string();
    println(s);
}

}