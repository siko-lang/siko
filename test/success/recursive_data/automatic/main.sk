module Main {

struct Foo {
    bar: Option[Record],
}

struct Record {
    other: Option[Record],
}

fn main() {
    let record = Record(None);
    let record2 = Record(Some(record));
    let foo = Foo(Some(record2));
}

}
