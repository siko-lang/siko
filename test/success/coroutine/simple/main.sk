module Main {

import Coroutine

fn simple(): co(Int) -> Int {
    yield 1;
    yield 2;
    return 3;
}

fn main() {
    let co1 : co(Int) -> Int = co simple();
    let (co1, res1) = resume(co1);
}

}