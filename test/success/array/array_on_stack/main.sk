module Main {

import NativePtr
import Array

fn main() {
    let arr: Array[U64, 10] = Array.uninitialized();
    assert(arr.len() == 10);
    assert(arr.sizeInBytes() == 10.toU64() * 8.toU64());
    arr.fill(0.toU64());
}

}