module Main {

struct Foo {
    bar: Int,
}

fn main() {
    let items = [Foo(0), Foo(1)];
    let numbers : Vec[Int] = items.iter().map(|x| x.bar).collect();
    for n in numbers {
        println("${n}");
    }
}

}