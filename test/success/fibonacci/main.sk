module Main {

fn fib(n: Int) -> Int {
    if n <= 0 {
        0
    } else if n == 1 {
        1
    } else {
        fib (n.clone() - 1) + fib(n.clone() - 2)
    }
}

fn main() {
    assert(fib(10) == 55);
}

}