module Main {

enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let c = Red;
    match (c, c) {
        (Red, Red) => (),
        (Green, Green) => (),
        (Blue, Blue) => (),
        _ => (),
    };
}

}