module Main {

enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let c = Red;
    let f = match (c, c) {
        (Red, Red) => Red,
        (Green, Green) => Green,
        (Blue, a) => a,
        _ => Blue,
    };
}

}