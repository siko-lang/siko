module Main {

struct Q {}

enum S {
    One(Q),
    Two(Q, Q),
    Zero
}

fn main() {
    let q1 : Q = Q();
    let q2 : Q = Q();
    let s1 : S = S.One(q1);
    let s2 : S = S.Two(q1, q2);
    let s3 : S = S.Zero;
}

}