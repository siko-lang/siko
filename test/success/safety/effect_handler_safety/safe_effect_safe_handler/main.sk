module Main {

effect Action {
    @safe
    fn perform() {
        let size = sizeof(Int);
    }
}

@safe
fn safe_handler() {
    let size = sizeof(Int);
    println("safe");
}

fn main() {
    with perform = safe_handler {
        perform();
    }
}

}
