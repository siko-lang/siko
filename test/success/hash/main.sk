module Main where

import Hash

data Record = { int :: Int, s :: String } deriving (Hash)

data Choice = One Int Int | Other String deriving (Hash)

main = do
    hasher <- create
    hasher <- hash "string" hasher
    hasher <- hash 128 hasher
    hasher <- hash (Record 1 "a") hasher
    hasher <- hash (One 1 2) hasher
    hash <- finish hasher
