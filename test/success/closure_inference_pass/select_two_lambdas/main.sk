module Main {

fn select(a: fn() -> (), b: fn() -> ()) -> fn() -> () {
    if True {
        a
    } else {
        b
    }
}

fn main() {
    let f1 = || {};
    let f2 = || {};
    let f = select(f1, f2);
}

}
