module Main {

@closure_alias
fn alias_nested(
    a: fn(fn() -> ()) -> (),
    b: fn(fn() -> ()) -> (),
) -> fn(fn() -> ()) -> () {
    b
}

fn main() {
    let c1 = |g| {
        g();
    };
    let c2 = |g| {
        g();
    };
    let c3 = alias_nested(c1, c2);
}

}
