module Main {

fn call_transform(f: fn(fn() -> ()) -> fn() -> (), arg: fn() -> ()) -> fn() -> () {
    f(arg)
}

fn main() {
    let transform = |g| {
        || {
            g();
        }
    };
    let arg = || {};
    let result = call_transform(transform, arg);
}

}
