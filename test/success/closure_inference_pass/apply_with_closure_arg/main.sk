module Main {

fn call_with_arg(f: fn(fn() -> ()) -> (), arg: fn() -> ()) {
    f(arg);
}

fn main() {
    let consumer = |g| {
        g();
    };
    let arg = || {};
    call_with_arg(consumer, arg);
}

}
