module Main {

struct Holder {
    f: fn() -> ()
}

fn make_holder(factory: fn() -> fn() -> ()) -> Holder {
    Holder(factory())
}

fn main() {
    let factory = || {
        || {}
    };
    let h = make_holder(factory);
}

}
