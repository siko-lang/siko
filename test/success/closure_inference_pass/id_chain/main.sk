module Main {

fn id[T](t: T) -> T {
    t
}

fn chain(n: Int) -> fn() -> () {
    let handler: fn() -> () = || {};
    let i = 0;
    while i < n {
        handler = || { handler(); };
        i = i + 1;
    }
    handler
}

fn main() {
    let f = id(chain(3));
}

}
