module Main {

struct Foo {
    value: Int
}

protocol Default[T] {
    fn default() -> T
}

impl DefImpl Default[Foo] {
    fn default() -> Foo {
        Foo(3)
    }
}

fn main() {
    let f: Foo = default();
    assert(f.value.eq(3));
}

}