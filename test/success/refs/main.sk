module Main {

fn foo(s: &String) {
    assert(s.eq("foo"));
    assert(s.eq("notfoo").not());
}

fn main() {
    let s = "foo";
    let ref : &String = &&&&&&s;
    let ref2 = &ref;
    foo(s);
    foo(&s);
    foo(&&&&s);
    //assert(s.eq(same));
}

}