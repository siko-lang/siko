module Main {

fn main() {
    let x = 5;
    let y = 10;
    let add = \a -> a + x;
    let add2 = \a -> a + x + y;
    let result = add(5);
    assert(result == 10);
    let result2 = add2(5);
    assert(result2 == 20);
}

}