module Main where

import Siko.MIR.Program
import Siko.Config
import Siko.MIR.Backend

main = do
    if True
    then ()
    else do
        config <- getDefaultConfig
        program <- createProgram
        lir_program <- processProgram config program
    ()
