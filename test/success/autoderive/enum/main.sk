module Main {

@derive(PartialEq, PartialOrd, Eq, Ord)
enum MyEnum[Q] {
    Foo(Q, Q),
    Bar,
}

fn main() {
    let e1 = Foo(1, 2);
    let e2 = Bar;
    //test partial eq
    assert(e1.ne(&e2));
    //testpartial ord
    assert(e1.partialCmp(&e2) == Some(Less));
    // test ord
    assert(e1.cmp(&e2) == Less);
}

}