module Main {

import Siko.Location.FileManager as FM
import Siko.Interner as I
import Siko.Compiler

fn main() {
    let mut interner = I.Interner.new();
    let mut fileManager = FM.FileManager.new();
    with I.interner = interner,
         FM.fileManager = fileManager {
        compile();
    }
}

}