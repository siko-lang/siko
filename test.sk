module Main {

enum Bool {
    True,
    False
}

enum Foo {
    Bar(Bool)
}

fn main() {
    match (True, 3, "4", Bar(True)) {
        (True,  1, "korte", Bar(True)) -> 1,
        (True,  alma, _, _) -> 2,
        _ -> 3,
    }
}

}