module Main {

enum Bool {
    True,
    False
}

enum FooBar {
    Foo(Bool),
    Bar(Bool, Bool),
}

fn main() {
    Foo(True);
    Bar(True,True);
    match Foo(True) {
        Bar(b, a) -> a,
        _ -> False
    };
}

}