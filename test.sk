module Bool {
enum Bool {
    True,
    False,
}
}

module Main {

struct Bar {

}

struct Foo {
    q : Quux,
}

struct Quux {
    b : Bar,
}

fn id[A](a: A) -> A {
    a
}

fn main() {
    let mut f = Foo(Quux(Bar()));
    {

    }
    if True {
        id (f.q.b);
        f.q.b = Bar();
    } else {
    }
    id (f.q.b);
}

}
