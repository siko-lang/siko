module Main {

enum Bool {
    True,
    False
}

enum FooBar {
    Foo(Bool),
    Bar(Bool, Bool),
}

fn main() {
    Foo(True);
    Bar(True,True);
    match Foo(True) {
        Bar(a, b) -> a,
        _ -> False
    };
}

}