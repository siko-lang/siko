module Std.Ops {

pub trait Clone[T] {
    fn clone(&self) -> T
}

pub trait Copy[T]

pub trait Drop[T] {
    fn drop(self) -> Self
}

}

module Bool {
pub enum Bool {
    True,
    False,
}
}

module Main {

struct Bar {

}

fn good() {}

fn bad() {}

fn main() {
    let b1 = Bar();
    {
        let b2 = Bar();
        loop {
            {
                let b3 = Bar();
                {
                    let b4 = Bar();
                    if True {
                        good();
                        break;
                    } else {
                        bad();
                        continue;
                    }
                }
            }
        }
    }
}

}
