module String {

class String {

}

}

module Bool {

class Bool {

}
}

module Unit {

class Unit {
}

}

module Main {

import Bool
import Unit
import String

class Address {
    city: String
    street: String

    fn check(self: Address) -> Unit {

    }
}

class Person {
    name: String
    address: Address
}

fn foo() -> Unit {

}

fn bar() -> Unit {
    
}

fn loop_init_fn() -> Unit {

}

fn id(address: Address) -> String {
    Unit();
    let a = Unit();
    let c = Unit();
    eat(c);
    let b = Unit();
    {
        let f = Unit();
        {
            if true {
                let address = Address(address.city, String());
                let person = Person(String(), address);
                person.address.city
            } else {
                let address = Address(String(), address.street);
                let person = Person(String(), address);
                person.address.city
            }
        }
    }
}

fn eat(a: Unit) -> Unit {

}

fn main() -> Unit {
    let address = Address(String(), String());
    let person = Person(String(), address);
    person.address.city;
    person.address;
}

}
     
