module Int {

class Int {
    fn add(self, other: Int) -> Int {
        addInt(self, other)
    }
}

fn addInt(first: Int, second: Int) -> Int = extern

}

module Other.Module {
    
fn siko_runtime_abort() = extern

fn siko_runtime_num(i: Int) = extern

}

module Main {

import Other.Module

fn main() {
    siko_runtime_abort();
    siko_runtime_num(6);
    let result = addInt(6, 4);
    let result2 = 5.add(5);
    siko_runtime_num(result);
    siko_runtime_num(result2);
}

}