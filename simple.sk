module Main where

import Map

data EExpr = EExpr Expr Int

data Expr = Foo EExpr
            | Bar

shift expr = case expr of
    Foo (EExpr _ _) -> println "Nothing"
    Bar -> println "F"

main = do
    subs <- chars "abc"
    shift Bar
    println subs