name: Master

on: [push]

jobs:
  linux-tests:

    runs-on: ubuntu-latest
    env:
      SIKO_ROOT: ${{ github.workspace }}
      SIKO_TARGET_OS: linux

    steps:
    - uses: actions/checkout@v4
      with:
          submodules: recursive
    - name: Install Boehm GC
      run: sudo apt-get install -y libgc-dev # valgrind
    - name: Bootstrap
      run: make base.bin
    - name: Compile compiler
      run: make siko.bin
    - name: Compile testrunner
      run: make runner.bin
      working-directory: ./
    - name: Run tests
      #run: make test-valgrind
      run: make test
      working-directory: ./

  macos-tests:

    runs-on: macos-latest
    env:
      SIKO_ROOT: ${{ github.workspace }}
      SIKO_TARGET_OS: macos
      HOMEBREW_REQUIRE_TAP_TRUST: 1

    steps:
    - uses: actions/checkout@v4
      with:
          submodules: recursive
    - name: Install Boehm GC
      run: brew install bdw-gc
    - name: Bootstrap
      run: make base.bin
    - name: Compile compiler
      run: make siko.bin
    - name: Compile testrunner
      run: make runner.bin
      working-directory: ./
    - name: Run tests
      run: make test
      working-directory: ./
