# Siko programming language.

VSCode support for [siko](https://github.com/siko-lang/siko).

When the package is not at the workspace root, configure its directory relative
to the workspace (or as an absolute path):

```json
"siko.projectPath": "siko/LSP"
```
