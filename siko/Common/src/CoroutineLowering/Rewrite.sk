module Siko.CoroutineLowering.Rewrite {

import Std.Map
import Siko.AST.Data
import Siko.AST.Function
import Siko.AST.Generics
import Siko.AST.Expr
import Siko.AST.Identifier
import Siko.AST.Name
import Siko.AST.Pattern
import Siko.AST.Statement as S
import Siko.AST.QualifiedName
import Siko.AST.Type
import Siko.AST.Annotations
import Siko.Location
import Siko.Utils.VarAllocator
import Siko.Utils.LabelAllocator
import Siko.Package.Collector
import Siko.Interner as I
import Siko.CoroutineLowering

pub trait Rewrite[T] {
    fn rewrite_co(t: T, ctx: CoroutineLowerer) -> T
}

instance Rewrite[Location]          = auto2(Map, =)
instance Rewrite[I.String]          = auto2(Map, =)
instance Rewrite[String]            = auto2(Map, =)
instance Rewrite[Int]               = auto2(Map, =)
instance Rewrite[AtomicOrdering]    = auto2(Map, =)
instance Rewrite[AtomicRmwOp]       = auto2(Map, =)
instance Rewrite[U8]                = auto2(Map, =)
instance Rewrite[Bool]              = auto2(Map, =)
instance Rewrite[QualifiedName]     = auto2(Map, =)
instance Rewrite[VariableName]      = auto2(Map, =)
instance Rewrite[Name]              = auto2(Map, =)
instance Rewrite[Identifier]        = auto2(Map, =)
instance Rewrite[FunctionKind]      = auto2(Map, =)
instance Rewrite[BinaryOp]          = auto2(Map, =)
instance Rewrite[UnaryOp]           = auto2(Map, =)
instance Rewrite[WitnessInfo]       = auto2(Map, =)
instance Rewrite[HandlerSelection]  = auto2(Map, =)
instance Rewrite[ImplicitRefInfo]   = auto2(Map, =)
instance Rewrite[CreateClosureInfo] = auto2(Map, =)
instance Rewrite[FunctionPtrInfo]   = auto2(Map, =)
instance Rewrite[LambdaArg]         = auto2(Map, =)
instance Rewrite[Pattern]           = auto2(Map)
instance Rewrite[PatternKind]       = auto2(Map)
instance Rewrite[DefaultValueInfo]  = auto2(Map, =)
instance Rewrite[Annotation]        = auto2(Map, =)
instance Rewrite[TypeArgDefs]       = auto2(Map, =)
instance Rewrite[MethodMap]         = auto2(Map, =)
instance Rewrite[VarAllocator]      = auto2(Map, =)
instance Rewrite[LabelAllocator]    = auto2(Map, =)
instance Rewrite[FunctionDefKind]   = auto2(Map, =)
instance Rewrite[SourcePackage]     = auto2(Map, =)
instance Rewrite[FunctionDef]       = auto2(Map)
instance Rewrite[FunctionArg]       = auto2(Map)
instance Rewrite[FieldDef]          = auto2(Map)
instance Rewrite[ItemInfo]          = auto2(Map)
instance Rewrite[VariantDef]        = auto2(Map)
instance Rewrite[StructDef]         = auto2(Map)
instance Rewrite[EnumDef]           = auto2(Map)
instance Rewrite[FunctionCallInfo]  = auto2(Map)
instance Rewrite[TypeAssociatedCallInfo] = auto2(Map, !)
instance Rewrite[NamedArgument]     = auto2(Map, =)
instance Rewrite[NamedPatternArg]   = auto2(Map, =)
instance Rewrite[NamedPatternInfo]  = auto2(Map)
instance Rewrite[MethodCallInfo]    = auto2(Map)
instance Rewrite[CallInfo]          = auto2(Map)
instance Rewrite[WithInfo]          = auto2(Map)
instance Rewrite[MatchArm]          = auto2(Map)
instance Rewrite[S.LetInfo]         = auto2(Map)
instance Rewrite[S.DeclareInfo]     = auto2(Map)
instance Rewrite[BlockInfo]         = auto2(Map)
instance Rewrite[S.Statement]       = auto2(Map)
instance Rewrite[ExprKind]          = auto2(Map)
instance Rewrite[MatchInfo]         = auto2(Map)
instance Rewrite[YieldInfo]         = auto2(Map)

instance Rewrite[TypeDef] {
    fn rewrite_co(ty: TypeDef, ctx: CoroutineLowerer) -> TypeDef {
        match ty {
            TypeDef.Co(CoroutineInfo(yield_ty, return_ty)) => {
                let yield_ty = rewrite_co(yield_ty, ctx);
                let return_ty = rewrite_co(return_ty, ctx);
                let info = ctx.get_or_create_co_enum(yield_ty, return_ty);
                TypeDef.Named(NamedInfo(Name.inner(info.wrap_qname), []))
            }
            TypeDef.Fn(ClosureInfo(args, ret, var)) => TypeDef.Fn(ClosureInfo(
                args.into_iter().map(|a| rewrite_co(a, ctx)).collect(),
                rewrite_co(ret, ctx),
                var,
            )),
            TypeDef.FnPtr(args, ret) => TypeDef.FnPtr(
                args.into_iter().map(|a| rewrite_co(a, ctx)).collect(),
                rewrite_co(ret, ctx),
            ),
            TypeDef.Named(NamedInfo(n, args)) => TypeDef.Named(NamedInfo(n, args)),
            TypeDef.Paren(inner)    => TypeDef.Paren(rewrite_co(inner, ctx)),
            TypeDef.Ptr(inner)      => TypeDef.Ptr(rewrite_co(inner, ctx)),
            TypeDef.Array(inner, n) => TypeDef.Array(rewrite_co(inner, ctx), n),
            TypeDef.Numeric(n)      => TypeDef.Numeric(n),
            TypeDef.Var(_)          => ty,
            TypeDef.Never(t)        => TypeDef.Never(t),
            TypeDef.Void            => TypeDef.Void,
            TypeDef.VoidPtr         => TypeDef.VoidPtr,
            TypeDef.Tuple(elems)    => TypeDef.Tuple(elems.into_iter().map(|e| rewrite_co(e, ctx)).collect()),
            TypeDef.Wildcard        => panic("lower_coroutines: unexpected Wildcard type"),
        }
    }
}

instance[T: Rewrite[T]] Rewrite[Option[T]] {
    fn rewrite_co(opt: Option[T], ctx: CoroutineLowerer) -> Option[T] {
        opt.map(|t| rewrite_co(t, ctx))
    }
}

instance[T: Rewrite[T]] Rewrite[Vec[T]] {
    fn rewrite_co(vec: Vec[T], ctx: CoroutineLowerer) -> Vec[T] {
        vec.into_iter().map(|t| rewrite_co(t, ctx)).collect()
    }
}

instance[K, V: Rewrite[V], Ord[K]] Rewrite[Map[K, V]] {
    fn rewrite_co(m: Map[K, V], ctx: CoroutineLowerer) -> Map[K, V] {
        m.iter().map(|(k, v)| (k, rewrite_co(v, ctx))).collect()
    }
}

instance Rewrite[Expr] {
    fn rewrite_co(expr: Expr, ctx: CoroutineLowerer) -> Expr {
        ctx.rewrite_expr(expr)
    }
}

}
