module Main where

import Std.Util

nextInt :: Int -> Option (Int, Int)
nextInt s = if s < 10 then Some (s + 1, s) else None

main = do
    iter <- createIterator 5 nextInt
    y <- 100
    sum <- iter |> filter (\x -> x != y) |> fold (\sum, i -> sum + i) 0
    assert (sum == 35)