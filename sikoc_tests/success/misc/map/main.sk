module Main where

import Std.Util
import Map

create :: Map Int Int
create = empty

main = do
    m <- create
    assert (show m == "{}")
    (m, _) <- insert m 1 2
    (m, _) <- insert m 3 4
    assert (show m == "{1: 2, 3: 4}")
    ()