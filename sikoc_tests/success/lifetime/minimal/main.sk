module Main where

import Hack

data Two = Two

data One = One Two

id a = (a, a)

func a = return a

main = do
    a <- 1
    b <- "alma"
    c <- 1.45
    d <- 'c'
    tr <- True
    if tr then () else ()
    t <- One Two
    list <- [t, t]
    sink (func t)
    sink list
    sink (id t)
    sink t
    ()
