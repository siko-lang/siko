module Main where

import Hack

data Inner = Inner
data Inner2 = Inner2

data Foo = One | Two Inner Inner2

data Data = { inner :: Inner, inner2 :: Inner2 }

id a = a

main = do
    d <- Data Inner Inner2
    d.inner2
    inner <- id d.inner
    d <- d { inner = inner }