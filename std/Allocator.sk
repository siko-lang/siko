module Allocator {

import Libc

pub effect Allocator {
    @unsafe
    fn allocate(size: U64) -> void* {
        Libc.malloc(size)
    }
    @unsafe
    fn deallocate(ptr: void*) {
        Libc.free(ptr);
    }
}

}