module Std.Basic.Util {

fn siko_runtime_abort() = extern
fn siko_runtime_num(v: Int) = extern

fn assert(v: Bool) {
    if v {
    } else {
        siko_runtime_abort();
    }
}

fn printNum(v: Int) {
    siko_runtime_num(v);
}

}