module Allocator {

import Libc.StdLib as StdLib

pub effect Allocator {
    @unsafe
    fn allocate(size: U64) -> void* {
        StdLib.malloc(size)
    }
    @unsafe
    fn deallocate(ptr: void*) {
        StdLib.free(ptr);
    }
}

}