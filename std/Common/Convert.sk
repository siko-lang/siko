@prelude
module Std.Convert {

pub trait Into[T, U] {
    fn into(value: T) -> U
}

instance[T] Into[T, T] {
    fn into(value: T) -> T { value }
}

pub trait From[T, U] {
    fn from(value: T) -> U
}

pub trait TryFrom[T, U] {
    fn tryFrom(value: T) -> Option[U]
}

}