@prelude
module Ordering {

@derive(PartialEq, Eq, PartialOrd, Ord, Show)
pub enum Ordering {
    Equal,
    Less,
    Greater
}

pub instance OrderingClone Clone[Ordering] {
    fn clone(&self) -> Ordering {
        match self {
            Equal -> Equal,
            Less -> Less,
            Greater -> Greater,
        }
    }
}

pub instance CopyOrdering Copy[Ordering]


}
