module Std.Sync.Atomic {

import Std.Sync.Atomic.Intrinsics as I

@derive(Clone, PartialEq, Eq)
pub enum Ordering {
    Relaxed,
    Release,
    Acquire,
    AcqRel,
    SeqCst,
}

@safe
pub fn fence(order: Ordering = SeqCst) {
    I.fence(order)
}

}
