module Std.Hash.Hasher {

import Std.Slice

// A Hasher incrementally consumes bytes and produces a U64 result.
pub trait Hasher[H] {
    fn write(hasher: H, bytes: Slice[U8])
    fn finish(hasher: H) -> U64
}

}
