module Std.Macro.Debug {

// Debug output channel for compile-time interpreted code. The
// @interpreter_test harness uses it, and future macro execution can use the
// same channel. Lower-builtins rejects it in normal compiled code.
@builtin
pub fn println(s: String) -> ()

}
