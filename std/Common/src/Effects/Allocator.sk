module Std.Effects.Allocator {

import Std.GC

@safe
pub fn default_allocate(size: U64) -> void* {
    gc_malloc(size)
}

pub effect Allocator {
    @safe
    fn allocate(size: U64) -> void* {
        default_allocate(size)
    }
}

}
