module Coroutine {

pub enum Result[Y, R] {
    Yielded(Y),
    Returned(R),
}

fn resume[Y, R, F](coro: co(Y, R) -> F, resumeValue: R) -> (co(Y, R) -> F,  Result[Y, R]) = extern

fn isCompleted[Y, R, F](coro: co(Y, R) -> F) -> Bool = extern

}