module Std.Ops.Basic {

pub protocol Clone[T] {
    fn clone(&self) -> T
}

pub protocol Copy[T]

pub protocol Drop[T] {
    fn drop(self) -> Self
}

pub protocol ImplicitConvert[T, U] {
    fn implicitConvert(self) -> U
}

}