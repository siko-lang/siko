module Std.Ops.Basic {

pub trait Clone[T] {
    fn clone(&self) -> T
}

pub protocol Copy[T]

pub trait Drop[T] {
    fn drop(self) -> Self
}

pub trait ImplicitConvert[T, U] {
    fn implicitConvert(self) -> U
}

}